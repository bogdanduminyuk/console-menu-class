#pragma once

#include <initializer_list>
#include <vector>
#include <string>
#include <iostream>

using namespace std;

class Menu
{

public:
    Menu();
    void run(vector<string>, vector<string(*)()>);
};

